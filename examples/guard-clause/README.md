# Пример для @atls/guard-clause

Здесь видно, как собираем `Guard` + `Against` и отстреливаем некорректные аргументы до того, как сервис полезет в домен.

Параметры помечаем декораторами, ловим `GuardErrors` и уже там решаем — падать, логировать или разворачивать 4xx наружу.
